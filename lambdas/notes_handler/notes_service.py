"""
CRUD operations for notes in DynamoDB.
"""

# TODO: Phase 3
# - create_note(user_id, title, content) -> note
# - get_notes(user_id) -> [notes]
# - get_note(user_id, note_id) -> note
# - update_note(user_id, note_id, title, content) -> note
# - delete_note(user_id, note_id) -> success